import type { ThemeUpdateOptions } from './types';

// ----------------------------------------------------------------------

/**
 * Escape hatch for one-off theme overrides that don't belong in core/ or
 * with-settings/. Empty by default.
 */
export const overridesTheme: ThemeUpdateOptions = {};
